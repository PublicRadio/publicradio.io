import {VK_LOGIN, VK_LOGOUT, VK_USER_INFO} from '../actions/vk'
export default function (state = {paused: true, currentTime: 0, currentTrack: null, currentStation: null}, action = {}) {
    switch (action.type) {
        default:
            return state
    }
}